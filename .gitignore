# Local env / secrets
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store

# Secrets & credentials — never commit these
*.pem
*.key
id_rsa*
.aws/
.ssh/
credentials.json
secrets.yaml
secrets.yml
token.json

# Node / Vite build artifacts
node_modules/
dist/
.shots/
*.local
.vercel/
